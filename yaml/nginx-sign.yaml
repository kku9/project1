apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-sign
  labels:
    app: nginx-sign
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-sign
  template:
    metadata:
      labels:
        app: nginx-sign
    spec:
      containers:
      - name: nginx
        image: songtaehoon/nginx-sign:v1.20
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "250m"
            memory: "250Mi"
          limits:
            cpu: "250m"
            memory: "250Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1  
---
apiVersion: v1
kind: Service
metadata:
  name: svc-sign
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30200
    protocol: TCP
  selector:
    app: nginx-sign
